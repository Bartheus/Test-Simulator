import { OptionBackgroundDirective } from './option-background.directive';

describe('OptionBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
